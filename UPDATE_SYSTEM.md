# نظام التحديثات التلقائي 🚀

## المميزات:
- ✅ فحص التحديثات تلقائياً عند فتح التطبيق
- ✅ عرض حوار يطلب التحديث
- ✅ إمكانية تحديث التطبيق دون حذفه
- ✅ دعم التحديثات الإجبارية (Forced Updates)

---

## طريقة الاستخدام:

### 1️⃣ إضافة بيانات التحديث في Firebase:

في **Firestore**، أنشئ collection باسم `appUpdates` وأضف document باسم `latestVersion`:

```json
{
  "version": "1.0.1-beta.1",
  "downloadUrl": "https://example.com/shadow-chat-update.apk",
  "message": "تطبيق جديد متاح مع إضافات رائعة!",
  "forced": false
}
```

### الحقول:
- **version**: الإصدار الجديد (مثل: `1.0.1-beta.1`)
- **downloadUrl**: رابط تحميل APK الجديد
- **message**: رسالة للمستخدم عن التحديث
- **forced**: هل التحديث إجباري؟ (true/false)

---

## شرح الكود:

### الدالة الرئيسية `checkForUpdates()`:
```dart
checkForUpdates(context)
```
- تتحقق من آخر إصدار في Firebase
- إذا كان هناك إصدار جديد، تعرض حوار التحديث

### مقارنة الإصدارات `_isNewVersionAvailable()`:
```dart
_isNewVersionAvailable("1.0.0", "1.0.1") // true
```
- تقارن بين الإصدار الحالي والجديد
- تدعم صيغ مختلفة (مثل: 1.0.0-beta.1)

---

## مثال عملي:

### النسخة الحالية: `1.0.0-beta.1`
### النسخة الجديدة: `1.0.1-beta.1`

عند فتح التطبيق:
1. يتحقق من Firebase
2. يرى أن هناك نسخة جديدة
3. يعرض حوار بالتفاصيل
4. المستخدم ينقر "تحديث الآن"
5. يتم تحميل APK الجديد

---

## ملاحظات مهمة:

⚠️ **التحديثات الإجبارية:**
- إذا كان `forced: true`، لا يستطيع المستخدم الضغط على "تحديث لاحقاً"
- يجب عليه التحديث فوراً

💾 **الحفظ التلقائي:**
- جميع البيانات والإعدادات تُحفظ تلقائياً
- التحديث لا يؤثر على بيانات المستخدم

🔔 **إخطار واحد فقط:**
- يتم التحقق من التحديثات عند فتح التطبيق فقط
- لا توجد إخطارات متكررة

---

## أمثلة إصدارات:

```
✓ 1.0.0-beta.1 → 1.0.1-beta.1 (تحديث)
✓ 1.0.0 → 1.0.1 (تحديث)
✓ 1.0.0 → 1.1.0 (تحديث)
✓ 0.9.9 → 1.0.0 (تحديث)
✗ 1.0.1 → 1.0.0 (لا تحديث - إصدار قديم)
```

---

## الحزم المستخدمة:

- **package_info_plus**: للحصول على إصدار التطبيق الحالي
- **url_launcher**: لفتح رابط التحميل
- **cloud_firestore**: لقراءة بيانات التحديث

